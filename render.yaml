services:
  - type: web
    name: fourrota-frontend
    runtime: static
    rootDir: frontend
    staticPublishPath: frontend/dist
    buildCommand: npm ci && npm run build
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
